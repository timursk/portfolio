import { Greetings } from '../components/common/Greetings';
import { SectionParagraph, SectionTitle, StyledSection } from '../components/common/styles';

export const Portfolio = () => {
  return (
    <>
      <Greetings>List of my pet projects!</Greetings>
      <StyledSection>
        <SectionTitle>Works</SectionTitle>
      </StyledSection>
    </>
  );
};
