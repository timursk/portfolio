import { SectionParagraph, SectionTitle, StyledSection } from '../components/common/styles';

export const Portfolio = () => {
  return (
    <StyledSection>
      <SectionTitle>Portfolio</SectionTitle>

      <SectionParagraph>List of my pet projects</SectionParagraph>
    </StyledSection>
  );
};
